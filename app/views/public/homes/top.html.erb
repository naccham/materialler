<div class="top">

  <div class="search-form-wrap">
    <div class="search-about">
      <div class="site-about">
        「materialler」は、素材サイトを探すためのサイトです。
      </div>
      <div id="search-form">
        <%= form_with url: search_sites_path, method: :get, local: true do |f| %>
            <%= f.text_field :title, placeholder: "",  class: "sbox" %>
            <%= f.submit "検索", class: "sbtn" %>
        <% end %>
      </div>
    </div>
  </div>

  <h1 class="title-text">CATEGORY</h1>

    <div class="categories">
        <div class="category">
          <div class="category-pic">
            <%= link_to tag_path(@tag1.id) do %>
              <%= image_tag 'pic.png' %>
            <% end %>
          </div>
          <p>
            <%= link_to tag_path(@tag1.id) do %>
              <%= @tag1.name %>
            <% end %>
          </p>
        </div>
        <div class="category">
          <div class="category-pic">
            <%= link_to tag_path(@tag2.id) do %>
              <%= image_tag 'illust.png' %>
            <% end %>
          </div>
          <p>
            <%= link_to tag_path(@tag2.id) do %>
              <%= @tag2.name %>
            <% end %>
          </p>
        </div>
        <div class="category">
          <div class="category-pic">
            <%= link_to tag_path(@tag3.id) do %>
              <%= image_tag 'icon.png' %>
            <% end %>
          </div>
          <p>
            <%= link_to tag_path(@tag3.id) do %>
              <%= @tag3.name %>
            <% end %>
          </p>
        </div>
        <div class="category">
          <div class="category-pic">
            <%= link_to tag_path(@tag4.id) do %>
              <%= image_tag 'silhouette.png' %>
            <% end %>
          </div>
          <p>
            <%= link_to tag_path(@tag4.id) do %>
              <%= @tag4.name %>
            <% end %>
          </p>
        </div>
        <div class="category">
          <div class="category-pic">
            <%= link_to tag_path(@tag5.id) do %>
              <%= image_tag 'texture.png' %>
            <% end %>
          </div>
          <p>
            <%= link_to tag_path(@tag5.id) do %>
              <%= @tag5.name %>
            <% end %>
          </p>
        </div>
        <div class="category">
          <div class="category-pic">
            <%= link_to tag_path(@tag6.id) do %>
              <%= image_tag 'pattern.png' %>
            <% end %>
          </div>
          <p>
            <%= link_to tag_path(@tag6.id) do %>
              <%= @tag6.name %>
            <% end %>
          </p>
        </div>
    </div>

  <h1 class="title-text">RANKING</h1>
  <div class="ranking">
    <%= render "public/sites/list", sites: @sites, thumbnail: @thumbnail %>

  </div>

</div>